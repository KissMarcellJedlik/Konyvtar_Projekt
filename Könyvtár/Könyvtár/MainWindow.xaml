<Window x:Class="ModernLibrary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library Manager" Height="600" Width="900">

    <Window.Resources>
        <Color x:Key="PrimaryColor">#6366f1</Color>
        <Color x:Key="SecondaryColor">#10b981</Color>
        <Color x:Key="DangerColor">#ef4444</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#e5e7eb"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Main content area -->
        <Grid Grid.Column="0" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Library Collection" 
                      FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <ListBox Grid.Row="1" x:Name="BooksListBox" Background="White"
                    BorderBrush="#e5e7eb" BorderThickness="1"
                    SelectionChanged="BooksListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" Margin="0,0,0,8" BorderBrush="#e5e7eb" BorderThickness="1">
                            <Grid Margin="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Author}" FontSize="12" Foreground="#666"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Edit" Margin="0,0,5,0" Padding="6,3"/>
                                    <Button Content="Delete" Background="{StaticResource DangerBrush}" 
                                            Click="DeleteButton_Click" Padding="6,3"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- Sidebar form -->
        <Border Grid.Column="1" Background="White" Margin="0,20,20,20" 
                BorderBrush="#e5e7eb" BorderThickness="1">
            <StackPanel Margin="20">
                <TextBlock Text="Add New Book" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                <TextBlock Text="Title" Margin="0,0,0,5"/>
                <TextBox x:Name="TitleTextBox" Margin="0,0,0,10"/>

                <TextBlock Text="Author" Margin="0,0,0,5"/>
                <TextBox x:Name="AuthorTextBox" Margin="0,0,0,10"/>

                <TextBlock Text="ISBN" Margin="0,0,0,5"/>
                <TextBox x:Name="IsbnTextBox" Margin="0,0,0,10"/>

                <TextBlock Text="Year" Margin="0,0,0,5"/>
                <TextBox x:Name="YearTextBox" Margin="0,0,0,10"/>

                <TextBlock Text="Genre" Margin="0,0,0,5"/>
                <TextBox x:Name="GenreTextBox" Margin="0,0,0,20"/>

                <Button x:Name="UpdateButton" Content="Update" Click="UpdateButton_Click" 
                       Margin="0,0,0,10" Visibility="Collapsed"/>
                <Button x:Name="AddButton" Content="Add Book" Click="AddButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>